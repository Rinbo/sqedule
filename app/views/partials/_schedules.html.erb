<h1 class="text-center mt-4">Schedules</h1>
<h4 class="text-center mt-20">Existing Schedules:<h4>
<% @schedules.each do |schedule| %>
  <p class="text-center text-bold mt-2"><%= link_to schedule.period, shedule_path(schedule) %><p>
<%end%>

<h3 class="text-center mt-20"><%= link_to "New Schedule", new_schedule_path %><h3>
