if (typeof displayElementSignup !== undefined) {
  displayElementSignup = document.getElementById('user-forms')
  loginForm = "<%= escape_javascript(render partial: 'partials/login') %>"
} else {
  let loginForm = "<%= escape_javascript(render partial: 'partials/login') %>"
  let displayElementSignup = document.getElementById('user-forms')
}
displayElementSignup.innerHTML = loginForm;

